from django.shortcuts import render,HttpResponse
from .models import *
# Create your views here.

# def test2(request):
    # 添加
    # result = Accounts.objects.create(username="123", password="123")
    # 查找
    # res = Accounts.objects.filter(username="123").first()
    # 修改
    # res = Accounts.objects.filter(username="123").first().update(password="321")
    # 删除
    # res = Accounts.objects.filter(username="123").first().delete()
    # return HttpResponse(res)
